name: Hello Kavana Workflow

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Print a message
        run: echo "🎉 Hello, Kavana! Your first GitHub Action ran successfully!"
